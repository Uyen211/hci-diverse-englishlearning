# prompt 1
Dự án này của mình đã có sẵn các file giao diện html tĩnh (tĩnh tức là chỉ có minh họa giao diện chưa có tương tác được vì vậy mình cần chuyển nó sang động, thành một dự án fe hoàn chỉnh. Đây là các công nghệ sử dụng của dự án này: code\package.json. Đầu tiên tôi muốn bạn đọc docs\design-rule\DESIGN.md - đây là phong cách design ta sẽ sử dụng vì vậy hãy thiết lập những cấu hình chung cần thiết trong  code. Tiếp theo tôi muốn bạn đọc trong docs\figma đây là folder chứa các file giao diện html tĩnh của tôi, hãy thực hiện Phát hiện các thành phần lặp lại sau đó  Tạo React component tương ứng (Import và dùng shadcn/ui nếu cấu trúc phù hợp). Hãy bắt đầu lập kế hoạch

# prompt 2
xem lại docs\design-rule\DESIGN.md cấu trúc các component như button,..... chỉnh sửa từ shadcn tải vào code\src\components\ui. sau đó chỉnh sửa lại sắp xếp các components cho phù hợp, đối với sidebar thì vì đây là hệ thống sẽ có hai tác nhân là học viên và admin, sidebar bạn đang để là của admin, cập nhật thêm sidebar học viên như trong docs\anh_raw\image.png. Tạo ra một cơ chế đăng nhập phù hợp -> tạo trang đăng nhập - có sẵn username password cố định (không cần đăng ký đâu) đầy dủ react hook form validate zod, sau khi đăng nhập thì có thể hiển thị ra màn hình (hiện tại chỉ có sidebar) phù hợp với role. tôi muốn bạn thiết lập cơ chế này trước. 